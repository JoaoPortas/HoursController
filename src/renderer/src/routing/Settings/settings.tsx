import ProfileDataSettings from "@renderer/views/Settings/ProfileData/ProfileDataSettings";

const settingsRoutes = [
    {
        path: '/settings/profileData',
        element: (<ProfileDataSettings />),
    }
];

export default settingsRoutes